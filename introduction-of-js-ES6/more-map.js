const numbers = [4,5,6,7,9];

// half of the numbers 

const half = numbers.map(num => num /2)
console.log(half)

const friends = [ 'asif','adil','kosum','siyam'];

const length = friends.map(nam =>nam[0])
  
console.log(length)